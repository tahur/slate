services:
  app:
    build: .
    ports:
      - "5173:5173"
    environment:
      OPENBILL_DB_PATH: data/openbill.db
      FRESH_DB: "1"
    volumes:
      - .:/app
      - /app/node_modules
    tmpfs:
      - /app/data
