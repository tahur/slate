# Baseline Metrics (Phase 0)

Date captured: February 13, 2026

## Commands and Results

1. `npm run check`
   - Result: pass
   - Diagnostics: `0 errors, 0 warnings`
   - Duration: `real 51.85s`

2. `npm run build`
   - Result: pass
   - Duration: `real 121.20s`

## Codebase Snapshot

1. Route server handlers (`+page.server.ts` and `+server.ts`): `48`
2. Server library files under `src/lib/server`: `33`

## Guardrail Baseline

1. Money math guardrail script: `npm run check:money`
2. Phase 1 regression guardrail script: `npm run check:phase1`
3. CI workflow configured at `.github/workflows/ci.yml`

## Notes

1. This baseline is used for refactor regression tracking.
2. Any future increase in typecheck errors above `0` is considered a release blocker.
