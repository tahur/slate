# Required
# Generate with:
# node -e "console.log(require('node:crypto').randomBytes(32).toString('hex'))"
BETTER_AUTH_SECRET=change-me-to-a-random-64-char-hex

# App URL — set this to your public URL in production
# SvelteKit adapter-node uses ORIGIN for CSRF protection
# Better Auth uses it as baseURL and auto-derives trustedOrigins
ORIGIN=http://localhost:3000

# Database (default: data/slate.db — no need to set unless custom path)
# SLATE_DB_PATH=data/slate.db

# Litestream backup (optional — set to replicate SQLite to cloud storage)
# LITESTREAM_REPLICA_URL=gcs://your-bucket/slate.db

# SMTP (optional, only needed for email features)
# SMTP_HOST=
# SMTP_USER=
# SMTP_PASS=
# SMTP_PORT=587
# SMTP_FROM=
# SMTP_SECURE=false
