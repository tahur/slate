# Required
# Generate with:
# node -e "console.log(require('node:crypto').randomBytes(32).toString('hex'))"
BETTER_AUTH_SECRET=change-me-to-a-random-64-char-hex

# App URL — set this to your public URL in production
# SvelteKit adapter-node uses ORIGIN for CSRF protection
# Better Auth uses it as baseURL and auto-derives trustedOrigins
ORIGIN=http://localhost:3000

# Database (Supabase pooler URL for app runtime)
# DATABASE_URL=postgresql://postgres.[ref]:[password]@aws-0-[region].pooler.supabase.com:6543/postgres?sslmode=require

# Optional separate URL for migration jobs (direct DB endpoint, usually 5432)
# DATABASE_URL_MIGRATION=postgresql://postgres:[password]@db.[ref].supabase.co:5432/postgres?sslmode=require

# Optional runtime DB tuning (keeps same Supabase pooler connection)
# DB_POOL_MAX=10
# DB_IDLE_TIMEOUT=20
# DB_CONNECT_TIMEOUT=10
# DB_PREPARE=false

# Supabase/DB notes
# - For postgres-js + Supabase pooler, runtime should use ssl + prepare:false in db client config.
# - RLS strategy should be explicit if non-service roles are used.

# SMTP (optional, only needed for email features)
# SMTP_HOST=
# SMTP_USER=
# SMTP_PASS=
# SMTP_PORT=587
# SMTP_FROM=
# SMTP_SECURE=false
