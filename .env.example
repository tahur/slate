# Required
BETTER_AUTH_SECRET=change-me-to-a-random-string

# App URL — set this to your public URL in production
# SvelteKit adapter-node uses ORIGIN for CSRF protection
# Better Auth uses it as baseURL for auth callbacks
ORIGIN=http://localhost:3000

# Database
OPENBILL_DB_PATH=data/openbill.db

# SMTP (optional, only needed for email features)
SMTP_HOST=
SMTP_USER=
SMTP_PASS=
SMTP_PORT=587
SMTP_FROM=
SMTP_SECURE=false
